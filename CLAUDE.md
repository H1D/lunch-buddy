# Claude Code Instructions

Refer to [AGENTS.md](AGENTS.md) for full project context, build commands, and testing guidelines.

## Pull Requests

This is a fork of `astiskala/lunch-buddy`. Always open PRs against this fork (`H1D/lunch-buddy`), NOT the upstream repo. Use `--repo H1D/lunch-buddy` with `gh pr create`.
